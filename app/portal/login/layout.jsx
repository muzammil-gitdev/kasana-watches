export const metadata = {
  title: "Login | Kasana Admin Portal",
  description: "Sign in to the Kasana Watches admin portal.",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
